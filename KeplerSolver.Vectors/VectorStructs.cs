using System;
using MathStructs;

namespace VectorStructs
{
	public class StateStructs
	{
		public struct Vector3SatellitesState
		{
			public Vector3 Position = new Vector3(x, y, z);
			public Vector3 Velocity = new Vector3(x, y ,z);
			
		}
	}
}
